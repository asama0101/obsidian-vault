# 議事録作成

`/meeting <プロジェクト名>`を実行する。

- `context`はプロジェクト名から自動設定される
- 同じプロジェクトの直近の議事録があれば、その未完了アクションアイテムを
  `## 前回の宿題・進捗確認`へ自動転記する（無ければ空欄）
- `/daily`のカレンダー連携から呼ばれた場合、`## 会議情報`にカレンダーの
  タイトル・URLが自動記載される（直接`/meeting`を呼んだ場合は空欄）
- 本文中の未チェックのアクションアイテムは、その場では自動タスク化されない。
  `/wrapup`実行時（1日の終わり）にまとめてタスク化される
