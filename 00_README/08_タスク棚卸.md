# タスク棚卸

`/task`を実行する。棚卸対象はフォルダを問わずvault全体の`type: task`ノート
（`30_Work`限定ではない）。

対話的に確認できる棚卸候補:
- 期限切れ・チェック日超過のタスク
- 長期放置タスク（`status`が`1_todo`のまま作成から30日以上経過）
- 優先順位未設定タスク（`due`・`check`がともに空欄）
- 未タスク化のアクションアイテム

`/audit`と違い、更新・削除・追加まで実際に手を入れられる。
