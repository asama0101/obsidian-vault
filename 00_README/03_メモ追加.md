# メモ追加

日次ノートの`## メモ`欄に直接書く（デフォルトの書き先）。

- 後から「単独ノートとして残したい・後で検索やリンクの対象にしたい」と判断したものだけ、
  「このメモを単独ノートにして」等と伝えて`/memo`（`.claude/skills/memo/SKILL.md`）で
  `10_Inbox`に切り出す（昇格操作）
- この判断は`/wrapup`実行時の棚卸でも提案され、承認すれば同じスキルで切り出される
