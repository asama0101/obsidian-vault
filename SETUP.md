# Obsidian vault 再設計 — 適用手順

`D:\Workspace\my-obsidian` に対して、この `task-control/` の内容を適用するための手順。既存構成は踏襲しない0ベース設計のため、フォルダのリネームとノートへの `type` プロパティ追記が必要。

## 前提

- Obsidian **1.9.10 以降**（Bases がコア機能として使えるバージョン）。`設定 → 一般` で確認する。
- 新規コミュニティプラグインは不要。Calendar・Git など既存のものはそのまま使ってよい。

## 手順チェックリスト

### 1. フォルダをリネームする（**必ずObsidianのファイルエクスプローラ上で**）
右クリック→名前変更、またはドラッグで移動する。OSのファイラーやGit経由で直接移動すると、既存ノート間のwikilinkが自動更新されずリンク切れするので絶対に避ける。

- `03_Task` → `30_Work`（既存タスクノートはそのままこの中に残る。あとで `type` を追記する）
- `04_Project` の中身を `30_Work` に統合する（1件ずつ移動、または`04_Project`を`30_Work`直下にドラッグしてマージ）
- `02_Diary` → `20_Diary`
- `05_Life` → `50_Life`
- `06_Knowledge` → `40_Knowledge`
- `07_Archive` → `60_Archive`
- `01_Inbox` → `10_Inbox`
- `99_Asset` → `90_Asset`
- `99_Template` → `92_Template`（中身は一旦残しつつ、後述のテンプレートファイルを追加）
- `99_WebClip` は廃止。中身が残っていれば `60_Archive` へ移すか削除し、`設定 → コアプラグイン → Webビューア` をオフにする

まず1つ（例: `05_Life → 50_Life`）だけ試し、そのフォルダにリンクしていた他のノートのリンクが壊れていないことを確認してから、残りを進める。

### 2. `91_Document` フォルダを新設する
vaultルート直下に作成する。PowerPoint・Excel・PDF・Word等のドキュメントをここに手動でドラッグ＆ドロップし、関連するノートから `[[ファイル名.pptx]]` でリンクする運用にする（画像は従来通り `90_Asset` に自動保存されるので分ける）。

### 3. `.base` ファイル・`00_TOP.md` をvaultルート直下に**個別配置**する
**重要:** `task-control` フォルダごとコピーしない（前回これで全Baseが0件表示になった）。以下のファイルだけを1つずつvaultルート直下に置く。

- [ ] `00_TOP.md`
- [ ] `タスクBase.base`
- [ ] `プロジェクトBase.base`
- [ ] `エリアBase.base`
- [ ] `議事録Base.base`
- [ ] `メモBase.base`

### 4. `92_Template` にテンプレートファイルを配置する
- [ ] `task.md` / `project.md` / `area.md` / `meeting.md` / `daily.md` を `92_Template/` にコピーする
- [ ] `設定 → コアプラグイン → テンプレート` を有効化し、テンプレートフォルダの場所を `92_Template` に設定する
- [ ] `設定 → ホットキー` で「テンプレート: テンプレートを挿入」に任意のキー（例: `Ctrl+Shift+T`）を割り当てる
- [ ] `設定 → コアプラグイン → デイリーノート` のテンプレートファイルを `92_Template/daily.md` に設定する

### 5. 既存ノートに `type` プロパティを追記する
`30_Work` に集まった既存のタスク・プロジェクトノートを開き、frontmatterに `type: task` または `type: project` を追記する。あわせて:

- 旧 `status`（例 `3_Running`、リスト形式）は `1_todo`/`2_doing`/`3_done` のいずれかの**単純な文字列**に書き直す
- 旧 `deadline` は `due` にリネーム
- `priority` を新規に入力する（今まで無かった項目）
- 旧 `target`・`tags`・`goal` は今回のスキーマにも残っているものはそのまま、無いものは削除して構わない
- プロジェクトに紐づくタスクには `context: "[[プロジェクト名]]"` を追記する
- `status` の値に見えない制御文字（`\r` 等）が混入していないか確認する。コピー＆ペーストで発生しやすい

継続的な運用業務（運用・保守など、終わりが無いもの）は `type: project` ではなく `type: area` にする（`92_Template/area.md` 参照）。

### 6. 動作確認
- [ ] `タスクBase.base` を開き、「すべて」「期限順」「優先度別」「コンテキスト別」の4タブが表示されエラーが無いことを確認する
- [ ] `プロジェクトBase.base`・`エリアBase.base`・`議事録Base.base` を開き、それぞれ対応するノートが表示されることを確認する
- [ ] `00_TOP.md` を開き（または `設定 → ファイルとリンク → 起動時に開くファイル` を `00_TOP` に変更して再起動時に）、5つの埋め込みビューが全て表示されることを確認する
- [ ] 試しに1つのタスクノートを `30_Work` 内の別のサブフォルダへ移動し、`タスクBase.base` の表示件数が変わらないことを確認する（`type`プロパティ基準でフォルダに依存しないことの確認）
- [ ] `sample-data/` のサンプル（`30_Work` 配下のProject・Area・Task・Meeting、`10_Inbox`/`20_Diary` のメモ）を実際にコピーして試すと、新スキーマでの見え方を先に確認できる

## 業務での使い方（操作手順）

- **メモを気軽に追加**: `Ctrl+N` → そのまま書く。frontmatterは付けない。探す時は全文検索（`Ctrl+Shift+F`）かタグペイン
- **タスクを1件追加**: `Ctrl+N` → タイトル入力 → テンプレート挿入で `task` → `status`/`priority`/`due`/`context` を埋める（空欄のままでもよい）
- **会議に出て議事録を取る**: 会議前に `Ctrl+N` → テンプレート挿入で `meeting`（`date`は自動で当日）→ 会議中は議題・決定事項を書き、アクションアイテムは `- [ ] [[新規タスク名]]` とリンクだけ書く → 会議後にリンクをクリックしてテンプレート挿入で `task` 化する
- **新しい案件が立ち上がる**: `Ctrl+N` → テンプレート挿入で `project` → `context` で紐づくタスク・議事録は自動的にバックリンクパネルと `タスクBase` の「コンテキスト別」ビューに集まる
- **障害対応・突発対応**: まずメモと同じ手順で書き留める。追跡が必要ならタスク化し、`context` に該当エリアをリンクする
- **リンクは `[[` の入力補完、または各ノート上部の「プロパティ」パネルから既存ノートを検索して選ぶ**と、タイプミスを防げて早い
- **メモをInboxに置きっぱなしにするのは問題ない**（検索・タグで見つかる）。対応が必要だと気づいたら、そのメモ自身に `type: task` 以下を追記するだけでよい（ファイル移動は不要）
- **1日の作業はデイリーノートで回す**: `92_Template/daily.md` に「今日のTodo」と期限順タスクの埋め込みビューが入っている。`00_TOP.md` が全体俯瞰、デイリーノートがその日の実行管理という役割分担

## 既知の制約
- `context` プロパティ（wikilink）での `groupBy` は、Obsidian側の実装により見え方が変わる可能性がある。実際に開いて意図通りグルーピングされるか確認すること
- プロジェクト/エリアごとの完了・未完了「件数」を数値で出す機能（rollup）は組み込んでいない。Bases の formula（`file.backlinks` 等）でも実現できる可能性があるが、手動リロードが必要など制約があるため見送った。必要になったら追加検討する
