---
description: 秘書ループを開始または更新する
---

`today` スキルを起動する。モードは現在のgitブランチと `Cabinet/Diary/<今日の日付>.md` の有無で判別する。`main` にいれば開始モード、当日の日次ブランチ `daily/<今日の日付>` にいて当日のデイリーノートがあれば更新モードとして扱う。当日以外の日次ブランチにいる場合は前日分が締められていないので、報告して停止する。判別表は `today` スキルの「モードの判別」節が正典。
