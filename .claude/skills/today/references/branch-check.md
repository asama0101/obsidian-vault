# today: 日次ブランチ確認ロジック

`opening.md`・`update.md`・`closing.md`から共通で参照される、日次ブランチ（`daily/<今日の日付>`）の確認・切り替え・作成ロジック。

## 入力

なし（今日の日付を使う）。

## 手順

1. 今日の日付の`daily/<今日の日付>`ブランチが既に存在すれば、チェックアウトする。
2. 存在しなければ、`main`から`git checkout -b daily/<今日の日付>`で新規作成する。

## 出力

確認・切替・作成した日次ブランチ名（`daily/<今日の日付>`）。
